apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wolfgang-rsync-upload-suite.fullname" . }}-test-connection"
  labels:
    {{- include "wolfgang-rsync-upload-suite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wolfgang-rsync-upload-suite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
